apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: whoami
  namespace: whoami
  labels:
    app.kubernetes.io/name: whoami
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: whoami
  endpoints:
  - port: metrics
